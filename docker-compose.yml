services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: assembly2026
      POSTGRES_USER: assembly
      POSTGRES_PASSWORD: assembly
    ports:
      - "5432:5432"
    volumes:
      - assembly_pg:/var/lib/postgresql/data
volumes:
  assembly_pg:
